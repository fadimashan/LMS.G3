@model LMS.Core.Entities.Activity

@{
    ViewData["Title"] = "Edit";
}

<h4 class="text-center text-muted m-2">Edit Activity</h4>

<hr />
<div class="row justify-content-center">
    <div class="col-auto">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label class="control-label"> Activity Type</label>
                <select id="selectItem1" asp-for="ActivityType" class="form-control">
                    <option value="Assignments">Assignments</option>
                    <option value="E_Learning">E_Learning</option>
                    <option value="lectures">lectures</option>
                    <option value="Other">Other</option>
                </select>
                <span asp-validation-for="ActivityType" class="text-danger"></span>
            </div>

         

            <div class="form-group">
                <label asp-for="StartDate" class="control-label"></label>
                <input asp-for="StartDate" class="form-control" />
                <span asp-validation-for="StartDate" class="text-danger"></span>
            </div>

            <div class="form-group" hidden="hidden">
                <input asp-for="ModuleId" class="form-control" />
            </div>

            <input type="hidden" asp-for="Module" />

            <div class="form-group">
                <label asp-for="EndDate" class="control-label"></label>
                <input asp-for="EndDate" class="form-control" />
                <span asp-validation-for="EndDate" class="text-danger"></span>
            </div>

            <div class="form-group" id="showList1">
                <label asp-for="Deadline" class="control-label"></label>
                <input asp-for="Deadline" class="form-control" />
                <span asp-validation-for="Deadline" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <input asp-for="Description" class="form-control" />
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-outline-danger" />
                <a asp-controller="Courses" asp-action="Index" class="btn btn-outline-info">Back to List</a>
            </div>

        </form>
    </div>
</div>

<script>

    let listOfItems = document.getElementById('selectItem1');

    listOfItems.addEventListener('change', () => {

        if (listOfItems.value === "E_Learning" || listOfItems.value === "lectures" || listOfItems.value === "Other") {
            inputBox.style.display = "none";
        } else {
            inputBox.style.display = "block";
        }
    });

    let inputBox = document.getElementById('showList1');
</script>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
